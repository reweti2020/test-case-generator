{
  "name": "test-case-generator",
  "version": "1.0.0",
  "description": "Generate test cases from any URL",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp src/index.html public/index.html",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "puppeteer-core": "^21.0.0",
    "chrome-aws-lambda": "^10.1.0",
    "@sparticuz/chromium": "^113.0.1"
  },
  "devDependencies": {
    "puppeteer": "^21.0.0",
    "vercel": "^29.1.1"
  },
  "engines": {
    "node": ">=16.x"
  }
}
