{
  "name": "test-case-generator",
  "version": "1.0.0",
  "description": "Generate test cases from any URL",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp index.html public/index.html",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "puppeteer-core": "^10.4.0",
    "chrome-aws-lambda": "^10.1.0",
    "@sparticuz/chromium-min": "^110.0.1"
  },
  "devDependencies": {
    "puppeteer": "^10.4.0",
    "vercel": "^29.1.1"
  },
  "engines": {
    "node": ">=16.x"
  }
}
